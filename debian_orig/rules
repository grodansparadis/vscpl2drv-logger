#!/usr/bin/make -f

%:
	dh $@ --with autoreconf

override_dh_auto_install:
	strip linux/vscpl2drv-logger.so
	install -D -m 0755 linux/vscpl2drv-logger.so $$(pwd)/debian/vscpl2drv-logger/srv/vscp/drivers/level2/vscpl2drv-logger.so
